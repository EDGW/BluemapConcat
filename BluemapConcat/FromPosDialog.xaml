<Window x:Class="BluemapConcat.FromPosDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BluemapConcat" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="From Position" Height="180" Width="370" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <d:Window.DataContext>
        <local:FromPosDialogViewModel/>
    </d:Window.DataContext>
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right">
            <Button Style="{StaticResource MaterialDesignOutlinedButton}" Width="100" Margin="5" Click="OnCancel">Cancel</Button>
            <Button Width="100" Margin="5" Click="OnOK">OK</Button>
        </StackPanel>
        <DockPanel Margin="5">
            <StackPanel Margin="5" Orientation="Horizontal" DockPanel.Dock="Bottom" Cursor="">
                <Label>Convert to tile index:</Label>
                <Label Content="{Binding X1}"/>
                <Label>to</Label>
                <Label Content="{Binding X2}"/>
            </StackPanel>
            <DockPanel Margin="5">
                <Label VerticalAlignment="Center" Margin="0,0,10,0">Position</Label>
                <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.Column="1" VerticalAlignment="Center">
                    <materialDesign:NumericUpDown Minimum="-65536" Maximum="65535" Value="{Binding Pos1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Padding="5,4,5,4"/>
                    <TextBlock VerticalAlignment="Center" Margin="5,0,5,0">to</TextBlock>
                    <materialDesign:NumericUpDown Minimum="-65536" Maximum="65535" Value="{Binding Pos2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Padding="5,4,5,4"/>
                </StackPanel>
            </DockPanel>
        </DockPanel>
    </DockPanel>
</Window>
